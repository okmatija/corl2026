# feedback/

Raw feedback exports (from the site's Feedback → Share/Copy button, or GitHub issues labelled `feedback`) can be saved here as
`YYYY-MM-DD-<name>.txt`. Claude merges them into `data/feedback.js` (shown on the site) and updates the plan.
